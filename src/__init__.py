# src/__init__.py
#
# This empty file marks the `src` folder as a Python "package."
# Without it, Python can't find modules inside this folder when you write:
#   from src.vision import VisionClient
#
# You don't need to put any code here — its presence alone is enough.
